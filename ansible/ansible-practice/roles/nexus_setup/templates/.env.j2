
# Nexus environment variables template

# nexus Docker image tag (e.g. v3.0, latest)
image_tag={{ nexus_image_tag }}

# Domain name used in nexus Host() routing rules
nexus_main_domain={{ nexus_main_domain }}

# Container restart policy (on-failure, always, unless-stopped)
restart_policy={{ restart_policy }}

# Nexus admin password (set via nexus_password variable)
nexus_admin_pass={{ nexus_password }}