apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster

nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: {{ awx_nodeport_port }}
        hostPort: {{ awx_nodeport_port }}
        listenAddress: {{ awx_nodeport_listen_address | to_json }}
        protocol: TCP
